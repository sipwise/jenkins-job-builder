wrappers:
  - vault-secrets:
      vault-url: 'http://127.0.0.1:8200'
      credentials-id: 'myCredentials'
      secrets:
        - secret:
            secret-path: 'secret/my-token'
            secret-values:
              - secret-value:
                  env-var: 'TOKEN'
                  vault-key: 'token'
