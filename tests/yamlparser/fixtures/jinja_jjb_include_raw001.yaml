- job-template:
    name: "test-job-{param}"
    builders:
        - shell: !j2: "should be value: {% jjb_include_raw 'jinja_jjb_include_raw001.sh' %}"

- project:
    name: test-include_jjb
    param: "value"
    jobs:
      - "test-job-{param}"
