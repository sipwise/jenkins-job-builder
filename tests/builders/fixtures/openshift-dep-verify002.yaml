builders:
  - openshift-dep-verify:
      api-url: https://openshift.default.svc.cluster.local
      dep-cfg: frontend
      name-space: test
      replica-count: 0
      auth-token: ose-key-dep-verify2